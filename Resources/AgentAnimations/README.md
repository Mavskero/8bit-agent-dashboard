桌宠动画 v5：七组，512×512，真实透明背景，完整透明 PNG 帧为运行时资源，无损 WebP 为备份。

打字：12 帧，左右手指轮流下压键盘，仅第 9 帧闭眼 100ms，眨眼占帧数 8.3%、占 2.85 秒周期时长约 3.5%。思考：18 帧，保持相同敲击节奏，并由脸颊侧依次出现气泡和灯泡，灯泡点亮后反向淡出。待机：双手完全静止，每 5.1 秒仅短暂眨眼 100ms。完成 OK：19 帧，保留抬手、转腕、OK 和指尖星光。品茶：12 帧，闭眼品尝停留约 1 秒，完整动作约 3.14 秒。

全部动作以笔记本上的四瓣标志为固定锚点校准，并锁定同一笔记本盖板轮廓；保留敲键盘、抬手、扶额、品茶等有意义的动作。打字、思考和待机逐帧复用同一人物主体，只有手指、眼睛和灵感图层按状态变化；完成动作的首尾帧也使用同一基准，避免状态切换时躯干与电脑发生轻微位移。应用显示时使用高质量透明插值生成平滑过渡。`manifest.json` 记录每帧时长、校准方式和边缘处理结果。

所有新增人物和特效图像由内置 imagegen 生成，`generation.json` 保存最终提示词；脚本负责拆帧、透明通道、动画图层合成和固定锚点校准。

应用运行时优先读取 `Frames/<action>/` 中的完整透明 PNG 帧，并使用 `manifest.json` 的逐帧时长。WebP 作为紧凑备份保留，不参与内置动作的正常播放，从而避免增量帧 disposal/blend 在动作切换时留下边缘残影。
